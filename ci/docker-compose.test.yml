services:
  sut:
    build:
      context: ..
      dockerfile: ci/Dockerfile
    image: ttvttm-dev
    working_dir: /workspace
    command: ["bash", "-lc", "./ci/dev-check.sh"]
    volumes:
      - ../:/workspace
